'use strict';

export default class Description {
    constructor(creator, cover) {
        const description = new creator('description', 'pre', 'class', 'description', '', 1, cover, false, '', '');
    }
}